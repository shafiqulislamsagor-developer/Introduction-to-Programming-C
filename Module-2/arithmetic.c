#include <stdio.h>
int main(){
    int a = 5;
    float b = 2;
    int sum = a + b;
    // printf("sum = %d\n",sum);
    int sub = a - b;
    // printf("sub = %d\n",sub);
    int mul = a * b;
    // printf("mul = %d\n",mul);
    float div = a / b;
    printf("div = %f\n",div);
    int mod = a % b;
    printf("mod = %d\n",mod);
    return 0;
}
